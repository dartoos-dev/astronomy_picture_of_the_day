name: astronomy_picture_of_the_day
description: A Flutter project to display NASA's astronomy picture of the day.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.2.0

flutter:
  uses-material-design: true
