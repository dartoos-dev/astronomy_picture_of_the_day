import 'package:flutter/material.dart';

class AstronomyPictureDetailsPage extends StatelessWidget {
  const AstronomyPictureDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Picture Details')),
      body: const Center(child: Text('Astronomy Picture Details Page')),
    );
  }
}
